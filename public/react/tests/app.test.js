import { App } from "../components/App";

describe('tests for App component', () => {
    test('checks form is initially not rendered', () => {
        
    })
    test('checks form renders after Add Item button is clicked', () => {

    })
    test('checks ItemsList is rendered on page load', () => {

    })
    test('checks ItemDetails renders after itemDetails state has content', () => {

    })
})